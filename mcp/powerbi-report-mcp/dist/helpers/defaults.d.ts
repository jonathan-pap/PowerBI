export declare const THEME_PRESETS: Record<string, {
    containerFormat: Array<{
        category: string;
        properties: Record<string, string | number | boolean>;
    }>;
    chartVisualFormat?: Array<{
        category: string;
        properties: Record<string, string | number | boolean>;
    }>;
    slicerContainerFormat?: Array<{
        category: string;
        properties: Record<string, string | number | boolean>;
    }>;
    dataColors?: string[];
}>;
